# Atlas Live Attempt 007 - bdd2593c

Date: 2026-06-22
Deployed commit: `bdd2593c feat(atlas): per-paragraph fallback basis markers`
Live base URL: `https://ai.alfydesign.com`
Status: live test completed; improved over attempt 006, still not a fully clean final report

## Trigger

The user said another set of changes had been committed to address the issues from attempt 006 and asked to repeat the same workflow: push first, deploy live, run one Atlas end-to-end test, inspect the result, log the attempt, and conclude.

## Local Push

- Local `main` was clean.
- `HEAD` and `origin/main` were already aligned at `bdd2593c`.
- `git push origin main` returned `Everything up-to-date`.

## Remote Deployment

- Remote checkout was already at `bdd2593c`.
- `./scripts/deploy.sh` pulled `main`, installed dependencies, verified migrations, applied migrations, built successfully, uploaded Sentry sourcemaps, patched body size, and restarted `langflow-chat.service`.
- Live health check after restart returned `{"status":"OK"}`.
- Service state after deployment and after the live test: `active`.
- Remote checkout still had pre-existing live-only files: modified `package-lock.json`, untracked `SELECT`, `atlas-query.cjs`, `eng.traineddata`, `hun.traineddata`, and `nld.traineddata`. They were not removed.
- Journal note: the previous service instance again reported `Failed with result 'timeout'` during restart, then a fresh process started and served the Atlas job.

## Test Prompt

Same regression prompt shape as attempts 004 through 006:

> Live Atlas regression check `<timestamp>`. Compare the best self-hosted embedding models for English technical-document retrieval in 2026. Use current web evidence. Include Executive Summary, model tradeoffs, deployment considerations, recommendation, and limitations. Keep the report concise but structured, and do not add decorative image galleries.

## Command Shape

```sh
LIVE_AI_BASE_URL=https://ai.alfydesign.com \
LIVE_AI_EMAIL=tester@tester.com \
LIVE_AI_PASSWORD=<redacted> \
LIVE_AI_OUTPUT_DIR=/private/tmp/atlas-live-regression-2026-06-22-bdd2593c \
LIVE_AI_TIMEOUT_MS=1200000 \
node /private/tmp/atlas-live-regression.mjs
```

## Live IDs And Artifacts

- Conversation: `e5bfb231-a676-45b6-9bce-8d4cc86617cd`
- Atlas job: `4850cf0f-d8a3-4027-a92d-3e1c8baf3dc8`
- File-production job: `b233aede-8a7b-427a-8b45-99ccc41d4370`
- Markdown file id: `b542043e-15c4-440c-9c6f-607dc55d1c12`
- HTML file id: `7e118d4d-a6f5-4592-92ff-aef92ae55dfa`
- PDF file id: `2c4fa443-50ed-4ec4-94ba-76cfe599ce83`
- Markdown artifact: `/private/tmp/atlas-live-regression-2026-06-22-bdd2593c/4850cf0f-d8a3-4027-a92d-3e1c8baf3dc8.md`
- HTML artifact: `/private/tmp/atlas-live-regression-2026-06-22-bdd2593c/4850cf0f-d8a3-4027-a92d-3e1c8baf3dc8.html`

## Live Automated Result

- Terminal status: success
- Atlas job status: `succeeded`
- Title: `Best Self-Hosted Embedding Models for English Technical-Document Retrieval (2026)`
- Accepted sources: 16
- Rejected sources: 85
- Token usage: 180,294 total
- Cost: 30,710 micros
- Top-level sections: 8 including source appendix
- Headings: 9
- Images: 2
- Inline basis markers: 20
- Standalone basis-marker blocks: 0
- Old deterministic fallback phrase: absent
- Executive source-stitch failure: absent
- Honest fallback `Evidence Summary`: absent
- `Additional Limitations`: absent
- `could not synthesize`: absent
- Recommendation heading: present
- Tradeoff heading: present
- Malformed headings: none

The regression script passed all current assertions.

## Manual Metrics

- Total words: 2,446
- Body words before source appendix: 1,879
- Source appendix words: 567
- Source share: 23.2%
- Image count: 2
- HTML inline basis markers: 20
- Markdown `> Basis:` blockquotes: 20
- Duplicate `## Limitations` headings: present
- Empty `Hardware fit:` bullet: absent
- `Top Self-Hosted Models.` plain-text pseudo-heading: absent
- Assemble-stage boilerplate: present (`Report generated at the Assemble stage. Use as a starting point for further investigation.`)

## Runtime Diagnostics

Service logs showed the job completed successfully, but token pressure is not gone:

- `[ATLAS_WRITER] Prompt truncated { originalLength: 53439, maxChars: 50000, profile: 'overview', evidenceCardCount: 16 }`
- Two late `NORMAL_CHAT_MODEL` calls finished with `finishReason: 'length'` at `outputTokens: 8000`.
- The live polling script repeatedly saw `apiRequestContext.get: socket hang up` while fetching conversation detail. The job continued through `coverage-review`, `synthesize`, `integrate`, `assemble`, `audit`, and `render`.

## Manual Assessment

This is another real improvement over attempt 006. The report now includes a proper `Model Tradeoffs` section, a fuller deployment section, a more substantial recommendation, more body content, and much denser paragraph-level Basis coverage in the rendered HTML. The body/source ratio is significantly better: source appendix share dropped from 34.5% to 23.2%.

However, I do not consider this fully fixed. The report still has assembly-quality defects that a user would notice:

- It has duplicate `## Limitations` headings.
- It includes the meta/boilerplate line `Report generated at the Assemble stage. Use as a starting point for further investigation.`
- In markdown, all 20 Basis markers are still rendered as blockquotes instead of being inline with paragraph text. The HTML renderer converts them to inline markers, so the UI is better than the markdown, but the markdown artifact is noisy.
- The writer prompt still truncates, though less than attempt 006.
- Two calls still hit `finishReason: 'length'`, now at the larger 8,000-token ceiling.
- Rejected sources increased from 58 to 85, so search/curation noise was worse despite better synthesis.

## Comparison With Attempt 006

| Metric | Attempt 006 (`34aa8a19`) | Attempt 007 (`bdd2593c`) | Direction |
|---|---:|---:|---|
| Automated regression status | passed | passed | same |
| Accepted sources | 16 | 16 | same |
| Rejected sources | 58 | 85 | worse |
| Total tokens | 132,679 | 180,294 | higher |
| Cost micros | 22,599 | 30,710 | higher |
| Total words | 1,714 | 2,446 | better |
| Body words | 1,123 | 1,879 | better |
| Source words | 591 | 567 | slightly better |
| Source share | 34.5% | 23.2% | better |
| Images | 1 | 2 | within requested cap |
| Inline basis markers in HTML | 1 | 20 | better |
| Standalone basis marker blocks in HTML | 0 | 0 | same/fine |
| Markdown basis blockquotes | not counted | 20 | noisy |
| Recommendation heading | present | present | same |
| Tradeoff heading | absent | present | fixed |
| Empty hardware bullet | present | absent | fixed |
| Plain-text pseudo-heading | present | absent | fixed |
| Duplicate limitations heading | absent | present | regression |
| Assemble-stage boilerplate | absent | present | regression |
| Writer prompt truncation | 58,511 -> 50,000 chars | 53,439 -> 50,000 chars | better but unresolved |
| Late finishReason length | 2 calls at 1,600 output tokens | 2 calls at 8,000 output tokens | cap raised, still length-stopped |

## Acceptance

Accepted as a further improvement over attempt 006, especially for body depth, tradeoff coverage, and HTML basis-marker placement. Rejected as a fully clean production-quality report because duplicate limitation sections, assemble-stage boilerplate, markdown basis blockquotes, remaining truncation, and length-stopped calls are still visible quality issues.
