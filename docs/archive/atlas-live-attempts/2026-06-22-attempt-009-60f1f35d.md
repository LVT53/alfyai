# Atlas Live Attempt 009 - 60f1f35d

Date: 2026-06-22
Deployed commit: `60f1f35d docs(atlas): add live attempt 008 log for final polish regression`
Live base URL: `https://ai.alfydesign.com`
Status: live test completed; main polish fixes reproduced, one length stop remains

## Trigger

The user asked to repeat the same workflow after another round of committed changes: push first, deploy latest `main`, run one Atlas end-to-end live regression, inspect the result, log the attempt, and conclude.

## Local Push

- Local `main` was clean.
- `HEAD` and `origin/main` were already aligned at `60f1f35d`.
- `git push origin main` returned `Everything up-to-date`.
- `60f1f35d` is a docs-only attempt-008 log commit on top of Atlas app-code commit `0d63d2b1`. The live app code under test is therefore the Wave 1-3 polish implementation from `0d63d2b1`, deployed at repository commit `60f1f35d`.

## Remote Deployment

- Remote checkout was already at `60f1f35d`.
- `./scripts/deploy.sh` pulled `main`, installed dependencies, verified migrations, applied migrations, built successfully, uploaded Sentry sourcemaps, patched body size, and restarted `langflow-chat.service`.
- Live health check after restart returned `{"status":"OK"}`.
- Service state after deployment and after the live test: `active`.
- Remote checkout still had pre-existing live-only files: modified `package-lock.json`, untracked `SELECT`, `atlas-query.cjs`, `eng.traineddata`, `hun.traineddata`, and `nld.traineddata`. They were not removed.
- Journal note: the previous service instance again reported `Failed with result 'timeout'` during restart, then a fresh process started and served the Atlas job.

## Test Prompt

Same standard regression prompt shape as attempts 004 through 007:

> Live Atlas regression check `<timestamp>`. Compare the best self-hosted embedding models for English technical-document retrieval in 2026. Use current web evidence. Include Executive Summary, model tradeoffs, deployment considerations, recommendation, and limitations. Keep the report concise but structured, and do not add decorative image galleries.

## Command Shape

```sh
LIVE_AI_BASE_URL=https://ai.alfydesign.com \
LIVE_AI_EMAIL=tester@tester.com \
LIVE_AI_PASSWORD=<redacted> \
LIVE_AI_OUTPUT_DIR=/private/tmp/atlas-live-regression-2026-06-22-60f1f35d-attempt-009 \
LIVE_AI_TIMEOUT_MS=1200000 \
node /private/tmp/atlas-live-regression.mjs
```

## Live IDs And Artifacts

- Conversation: `f8ce9597-dda4-43e0-8019-343f9be12df1`
- Atlas job: `a9fbf65c-e6d2-40cf-ad23-5d337ec10cde`
- File-production job: `24174f57-9d3d-4519-9e80-7f9fa871fffd`
- Markdown file id: `a82465cf-4236-4237-bbe1-c30768129f69`
- HTML file id: `8f549702-f624-474d-bfc2-a260d02de639`
- PDF file id: `655cc04e-999b-49f3-8da2-f6b242b67cd0`
- Markdown artifact: `/private/tmp/atlas-live-regression-2026-06-22-60f1f35d-attempt-009/a9fbf65c-e6d2-40cf-ad23-5d337ec10cde.md`
- HTML artifact: `/private/tmp/atlas-live-regression-2026-06-22-60f1f35d-attempt-009/a9fbf65c-e6d2-40cf-ad23-5d337ec10cde.html`

## Live Automated Result

- Terminal status: success
- Atlas job status: `succeeded`
- Title: `Self-Hosted Embedding Models for English Technical-Document Retrieval (2026)`
- Accepted sources: 16
- Rejected sources: 59
- Token usage: 146,851 total
- Cost: 26,501 micros
- Top-level sections: 7 including source appendix
- Headings: 8
- Images: 1
- Inline basis markers in HTML: 14
- Standalone basis-marker blocks: 0
- Old deterministic fallback phrase: absent
- Executive source-stitch failure: absent
- Honest fallback `Evidence Summary`: absent
- `Additional Limitations`: absent
- `could not synthesize`: absent
- Recommendation heading: present
- Tradeoff heading: present
- Malformed headings: none

The standard regression script passed all current assertions.

## Manual Metrics

- Total words: 2,125
- Body words before source appendix: 1,554
- Source appendix words: 571
- Source share: 26.9%
- Image count: 1
- HTML inline basis markers: 14
- Markdown inline `(Basis: ...)` markers: 14
- Markdown `> Basis:` blockquotes: 0
- Supported basis markers: 4
- Partial basis markers: 10
- Duplicate `## Limitations` headings: absent, exactly one
- Empty `Hardware fit:` bullet: absent
- `Top Self-Hosted Models.` plain-text pseudo-heading: absent
- Assemble-stage boilerplate: absent
- Truncated basis-marker text: present once (`so this marker is...)`)

## Runtime Diagnostics

Service logs showed the job completed successfully, with writer+audit claim-basis integration active:

- `[ATLAS_WRITER] Prompt truncated { originalLength: 61023, maxChars: 50000, profile: 'overview', evidenceCardCount: 16 }`
- `[ATLAS] Claim basis source: writer+audit (4 writer entries, 10 merged total)`
- `[ATLAS] Claim basis source: writer+audit (4 writer entries, 14 merged total)`
- One `NORMAL_CHAT_MODEL` call finished with `finishReason: 'length'`, `inputTokens: 6392`, `outputTokens: 12000`.
- The live polling script repeatedly saw `apiRequestContext.get: socket hang up` while fetching conversation detail. The job continued through `coverage-review`, `synthesize`, `integrate`, `assemble`, `audit`, and `render`.

## Manual Assessment

This run confirms the main Wave 1-3 polish fixes still work under the standard regression harness:

- No markdown `> Basis:` blockquotes.
- Basis markers are inline parentheticals in markdown and inline markers in HTML.
- No duplicate `## Limitations`.
- No assemble-stage boilerplate.
- No old fallback report.
- No malformed headings.
- Model tradeoffs, deployment considerations, recommendation, and limitations all appear as real sections.

The actual report is more useful than the old sparse/fallback outputs. It makes a clear recommendation for BGE-M3, discusses model tradeoffs, identifies benchmark heterogeneity, and preserves concrete benchmark caveats. It is also stronger than attempt 008 on body length and source ratio.

I would still not call the pipeline completely clean. Remaining issues:

- One basis marker appears visibly truncated in markdown: `so this marker is...)`.
- Writer prompt truncation is heavier than attempt 008's reported run, at `61,023 -> 50,000` chars.
- One model call still hit the 12,000-token output ceiling with `finishReason: 'length'`.
- Rejected sources increased from 44 in attempt 008 to 59 here.
- The selected image is only loosely relevant and still looks like a generic embedding article image rather than strong evidence-specific visual support.

## Comparison With Attempt 008

| Metric | Attempt 008 (`0d63d2b1`, external log) | Attempt 009 (`60f1f35d`) | Direction |
|---|---:|---:|---|
| Automated regression status | succeeded | succeeded | same |
| Accepted sources | 16 | 16 | same |
| Rejected sources | 44 | 59 | worse |
| Total tokens | 94,652 | 146,851 | higher |
| Cost micros | 16,200 | 26,501 | higher |
| Total words | 1,645 | 2,125 | better |
| Body words | 1,136 | 1,554 | better |
| Source words | 509 | 571 | slightly higher |
| Source share | 30.9% | 26.9% | better |
| Images | 1 | 1 | same |
| Inline basis markers | 12 | 14 | slightly better |
| Markdown `> Basis:` blockquotes | 0 | 0 | fixed/same |
| Duplicate Limitations heading | absent | absent | fixed/same |
| Assemble-stage boilerplate | absent | absent | fixed/same |
| Tradeoff heading | present | present | same |
| `finishReason: 'length'` | 0 reported | 1 call at 12,000 output tokens | worse |
| Writer prompt truncation | present | 61,023 -> 50,000 chars | still present |
| Writer+audit integration | active, 4+8=12 | active, 4+10=14 | working |

## Acceptance

Accepted as a successful reproduction of the important final-polish fixes and a usable production-quality report shape for this prompt. Not accepted as perfect: the visible truncated basis marker, remaining writer prompt truncation, one length-stopped call, repeated socket hangups during polling, and weaker image relevance are still worth tracking.
